  syntax_check:
    script:
      - export WORKON_HOME=$HOME/.virtualenvs
      - export PROJECT_HOME=$HOME/projects
      - source /usr/share/virtualenvwrapper/virtualenvwrapper.sh
      - mkvirtualenv -p `which python3` pylint3
      - pip install pylint3
      - pylint --disable=F0401 *.py
